name: flutter_event_app
description: A modern Flutter event management application.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.2.0
  riverpod: ^2.4.0
  flutter_riverpod: ^2.4.0
  image_picker: ^1.0.4
  intl: ^0.18.0
  go_router: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
